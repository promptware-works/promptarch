{
  "name": "@promptarch/tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PROMPTARCH repository tooling (TypeScript/Node per ADR-003).",
  "scripts": {
    "check:digests": "node digests/check-digests.ts",
    "check:registry": "node registry/check-registry.ts",
    "check:graph": "node graph/check-graph.ts",
    "release": "node release.ts"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
